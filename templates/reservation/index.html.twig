{% extends 'base.html.twig' %}

{% block title %}Réservation{% endblock %}

{% block body %}
<div class="mx-3">
    {{form_start(form)}}
    {{form_rest(form)}}
    <button class="btn btn-primary mb-3">Réserver</button>
    {{form_end(form)}}
</div>
{% endblock %}
